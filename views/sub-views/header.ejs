<nav class="navbar site-navbar fixed-top container col-lg-8 mx-auto">
  <%
    var currentSearch = '';
    if (req && req.query && req.query.q)
      currentSearch = req.query.q;
  %>
  <div class="row g-0 site-navbar-main">
    
    <header class="col col-md-4 d-flex site-header-brand">
      <a href="<%= utils.urlFor(req, '/') %>"><img class="logo me-2" src="<%= site.logoVector %>" width="56" height="56"></a>
      <div>
        <h6 class="title fs-4 mb-0">
          <a href="<%= utils.urlFor(req, '/') %>" class="text-dark text-decoration-none">
          <% if ((page.context.book && page.context.book.alias === 'quran') || page.context.quranSearchProxy) { %>
            Quran Unlocked
          <% } else { %>
            <%= site.name %>
          <% } %>
          </a>
        </h6>
        <% if (page.subtitle_en) { %>
        <div class="subtitle fs-6 text-muted">
          <%- page.subtitle_en %>
          <%- page.subtitle ? `<span lang="ar">${page.subtitle}</span>` : '' %>
        </div>
        <% } %>
      </div>
    </header>

    <menu class="col d-md-block d-none">
      
      <ul class="nav container-fluid justify-content-end">
        <li class="nav-item"><a class="nav-link" href="<%= utils.urlFor(req, '/') %>">Home</a></li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">Books</a>
          <ul class="dropdown-menu dropdown-menu-end">
            <li><a class="dropdown-item" href="<%= utils.quranUrl(req, '/quran') %>"><i class="bi bi-book"></i> Quran</a></li>
            <li><a class="dropdown-item" href="<%= utils.urlFor(req, '/books') %>"><i class="bi bi-book"></i> Ḥadīth</a></li>
            <li><a class="dropdown-item" href="<%= utils.quranUrl(req, '/quran/tafsir') %>"><i class="bi bi-journal-text"></i> Tafsīr</a></li>
          </ul>
        </li>
        <li class="nav-item"><a class="nav-link" href="<%= utils.quranUrl(req, '/quran') %>">Quran</a></li>
        <li class="nav-item"><a class="nav-link" href="<%= utils.urlFor(req, '/blog') %>">Blog</a></li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">More</a>
          <ul class="dropdown-menu dropdown-menu-end">
            <li><a class="dropdown-item" href="<%= utils.urlFor(req, '/bookmarks') %>"><i class="bi bi-bookmark"></i> My Bookmarks</a></li>
            <li><a class="dropdown-item" href="<%= utils.urlFor(req, '/liked') %>"><i class="bi bi-heart"></i> Liked</a></li>
            <li><a class="dropdown-item" href="<%= utils.urlFor(req, '/commented') %>"><i class="bi bi-chat-right-text"></i> Latest Reflections</a></li>
            <li><a class="dropdown-item" href="<%= utils.urlFor(req, '/highlights') %>"><i class="bi bi-star"></i> Highlighted</a></li>
            <li><a class="dropdown-item" href="<%= utils.urlFor(req, '/titled') %>"><i class="bi bi-card-heading"></i> Titled</a></li>
          </ul>
        </li>
        <li class="nav-item">
          <a class="nav-link top-nav-icon-link" href="<%= utils.urlFor(req, '/liked') %>" title="Favorites" aria-label="Favorites">
            <i class="bi bi-heart"></i>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link top-nav-icon-link" href="<%= utils.urlFor(req, '/bookmarks') %>" title="Bookmarks" aria-label="Bookmarks">
            <i class="bi bi-bookmark"></i>
          </a>
        </li>
        <% if (site.admin) { %>
        <li class="nav-item edit-gear"><a class="nav-link" onclick="var editMode=(document.cookie ? document.cookie.match(/editMode=1/) : false); editMode ? document.cookie='editMode=0;path=/;max-age=7776000;samesite=lax' : document.cookie='editMode=1;path=/;max-age=7776000;samesite=lax'; console.log(document.cookie); location.reload();">
            <i class="bi bi-gear<%=site.editMode ? '-fill' : ''%>"></i></a>
        </li>
        <% } %>
        <li class="nav-item search-click-toggle">
          <form action="/">
          <input role="search" type="text" class="form-control search-click" name="q" placeholder="Search hadīth, āyāt, or tafāsīr..." value="<%= currentSearch %>" />
          </form>
          <a class="nav-link"><i class="bi bi-search"></i></a>
        </li>
        <li class="nav-item">
          <a type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvas-topnav">
            <i id="icon-menu" class="bi bi-list"></i>
          </a>
        </li>
        <li class="nav-item d-flex align-items-center" id="nav-auth-desktop"></li>
      </ul>
      
    </menu>

    <menu class="col-auto d-md-none d-block">
      <div class="nav container-fluid justify-content-end align-items-center gap-2 top-nav-mobile-actions">
        <button type="button" class="btn btn-sm nav-auth-btn" id="nav-auth-mobile">Sign in</button>
        <a type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvas-topnav">
          <i id="icon-menu" class="bi bi-list"></i>
        </a>
      </div>
    </menu>

    <nav id="offcanvas-topnav" class="offcanvas offcanvas-start collapse" tabindex="-1" data-bs-static="true" data-bs-backdrop="true">
      <div class="offcanvas-header">
        <h5 class="offcanvas-title fs-4" id="offcanvasLabel">Menu</h5>
        <button type="button" class="btn-close" data-bs-dismiss="offcanvas"></button>
      </div>
      <div class="offcanvas-body">
        <form action="/">
          <input role="search" type="text" class="form-control mb-2" name="q" placeholder="Search hadīth, āyāt, or tafāsīr..." value="<%= currentSearch %>" />
        </form>
        <div class="row">
          <ul class="col-7 nav flex-column offcanvas-col1">
            <li class="nav-item"><a class="nav-link" href="<%= utils.urlFor(req, '/') %>"><i class="bi bi-house"></i> Home</a></li>
            <li class="nav-item"><a class="nav-link" href="<%= utils.quranUrl(req, '/quran') %>"><i class="bi bi-book"></i> Quran</a></li>
            <li class="nav-item"><a class="nav-link" href="<%= utils.urlFor(req, '/books') %>"><i class="bi bi-book"></i> Ḥadīth Books</a></li>
            <li class="nav-item"><a class="nav-link" href="<%= utils.quranUrl(req, '/quran/tafsir') %>"><i class="bi bi-journal-text"></i> Tafsīr Books</a></li>
            <li class="nav-item"><a class="nav-link" href="<%= utils.urlFor(req, '/blog') %>"><i class="bi bi-text-paragraph"></i> Blog</a></li>
            <li><hr></li>
            <li class="nav-item"><a class="nav-link" href="<%= utils.urlFor(req, '/bookmarks') %>"><i class="bi bi-bookmark"></i> My Bookmarks</a></li>
            <li class="nav-item"><a class="nav-link" href="<%= utils.urlFor(req, '/liked') %>"><i class="bi bi-heart"></i> Liked</a></li>
            <li class="nav-item"><a class="nav-link" href="<%= utils.urlFor(req, '/commented') %>"><i class="bi bi-chat-right-text"></i> Latest Reflections</a></li>
            <li class="nav-item"><a class="nav-link" href="<%= utils.urlFor(req, '/highlights') %>"><i class="bi bi-star"></i> Highlighted</a></li>
            <li class="nav-item"><a class="nav-link" href="<%= utils.urlFor(req, '/titled') %>"><i class="bi bi-card-heading"></i> Titled</a></li>
            <% if (site.admin) { %>
              <li><hr></li>
              <li class="nav-item edit-gear"><a class="nav-link" onclick="var editMode=(document.cookie ? document.cookie.match(/editMode=1/) : false); editMode ? document.cookie='editMode=0;path=/;max-age=7776000;samesite=lax' : document.cookie='editMode=1;path=/;max-age=7776000;samesite=lax'; console.log(document.cookie); location.reload();">
                <% if (site.editMode) { %>
                  <i class="bi bi-gear-fill"></i> <strong>View</strong>
                <% } else { %>
                  <i class="bi bi-gear"></i> <strong>Edit</strong>
                <% } %>
              </a></li>
              <% } %>
          </ul>
          <ul class="col-5 nav flex-column offcanvas-col2">
            <% for (b of books.filter(b => !b.hidden && b.alias !== 'quran')) { %>
              <li class="nav-item"><a class="nav-link" title="<%= b.shortName %>" href="<%= utils.urlFor(req, `/${b.alias}`) %>"><%= b.shortName_en %></a></li>
            <% } %>
          </ul>
        </div>
      </div>
    </nav>
    
  </div>
  
  <% if (page.menu === 'Chapter' || page.menu === 'Section') { 
      var book = page.context.book;
      var chapter = page.context.chapter;
      var sections = chapter.sections; 
      var isQuranToc = book && book.alias === 'quran';
  %>
  <nav class="row g-0 chapter-toc">
    <%# --- TABLE OF CONTENTS --- %>
    <div class="col-12 d-flex justify-content-center">
      <% if (sections && sections.length > 0) { %>
        <a data-bs-toggle="collapse" href="#toc2" title="Click to show sections">Section Menu <i class="bi bi-caret-down-square"></i></a>
        <% if (page.context.book && page.context.book.alias === 'quran' && (page.menu === 'Chapter' || page.menu === 'Section')) {
             var isPassage = req.query.passage != undefined || page.context.passage || (page.menu === 'Section' && req.query.ayat == undefined);
             if (!isPassage) { %>
                &nbsp;|&nbsp; <a href="<%= page.menu === 'Section' ? req.path : `${req.path}?passage` %>" title="Passage View"><i class="bi bi-text-paragraph"></i> Passage</a>
             <% } else { %>
              &nbsp;|&nbsp; <a href="<%= utils.urlFor(req, `/${(page.context.section || page.context.chapter).path}?ayat`) %>" title="Āyah View"><i class="bi bi-blockquote-left"></i> Āyāt</a>
        <%   }
           } %>
      <% } else { %>
        <!-- <a>No sections in this chapter</a> -->
      <% } %>
    </div>
    <div id="toc2" class="col-12 toc2 collapse">
      <table class="table">
        <tbody>
          <%
				for (var section of sections) {
					if (section.level < 3) {
						title_en = utils.truncate(utils.emptyIfNull(section.title_en).toString(), 75);
						title = utils.truncate(utils.emptyIfNull(section.title).toString(), 150); %>
          <tr class="title">
            <% if (!isQuranToc) { %>
            <td><%- section.start %></td>
            <% } %>
            <% if (section.start) { %>
            <td>
              <a href="<%= utils.urlFor(req, `/${section.path}`) %>">
                <%= `§${section.h2 + (section.h3 ? `-${section.h3}` : '')}` %>
                <%- title_en || `Section ${section.h2 + (section.h3 ? `-${section.h3}` : '')}` %> (<%=section.count%>)
              </a>
              <% if (page.context.book && page.context.book.alias === 'quran') { %>
                &nbsp;<a href="<%= utils.urlFor(req, `/${section.path}`) %>" title="Passage View"><i class="bi bi-text-paragraph"></i></a>
              <% } %>
            </td>
            <td lang="ar"><a href="<%= utils.urlFor(req, `/${section.path}`) %>"><%- title %></a></td>
            <% } else { %>
            <td><%- title_en || 'Section' %></td>
            <td lang="ar"><%- title %></td>
            <% } %>
          </tr>
          <%    }
					}  %>
        </tbody>
      </table>
    </div>
  </nav>
  <% } %>

  <% if (page.menu === 'Tafsir' && page.context.tafsirSectionMenu && page.context.tafsirSectionMenu.length > 0) { %>
  <nav class="row g-0 chapter-toc tafsir-section-menu">
    <div class="col-12 d-flex justify-content-center">
      <a data-bs-toggle="collapse" href="#toc2" title="Click to show sections">Section Menu <i class="bi bi-caret-down-square"></i></a>
    </div>
    <div id="toc2" class="col-12 toc2 collapse">
      <table class="table">
        <tbody>
          <% for (var tafsirSection of page.context.tafsirSectionMenu) { %>
          <tr class="title">
            <td>
              <a href="<%= tafsirSection.url %>">
                §<%= tafsirSection.index %>
                <span><%= tafsirSection.ayahRangeLabel %> <%= tafsirSection.rangeLabel %></span>
              </a>
            </td>
            <td>
              <a href="<%= tafsirSection.url %>"><%- tafsirSection.title_en || 'Section' %></a>
            </td>
            <td lang="ar" dir="rtl">
              <a href="<%= tafsirSection.url %>">
                <%- tafsirSection.title || 'باب' %>
                <span><%= tafsirSection.rangeLabelAr %></span>
              </a>
            </td>
          </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </nav>
  <% } %>
  <% if (page.menu === 'Tafsir' && page.context.tafsirSectionMenuUrl && !(page.context.tafsirSectionMenu && page.context.tafsirSectionMenu.length > 0)) { %>
  <nav class="row g-0 chapter-toc tafsir-section-menu" data-tafsir-section-menu-url="<%= page.context.tafsirSectionMenuUrl %>">
    <div class="col-12 d-flex justify-content-center">
      <a data-bs-toggle="collapse" href="#toc2" title="Click to show sections">Section Menu <i class="bi bi-caret-down-square"></i></a>
    </div>
    <div id="toc2" class="col-12 toc2 collapse">
      <table class="table">
        <tbody>
          <tr class="title tafsir-section-menu-status">
            <td colspan="3" class="text-center text-muted">Loading sections...</td>
          </tr>
        </tbody>
      </table>
    </div>
  </nav>
  <% } %>
  
</nav>
