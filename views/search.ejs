<!DOCTYPE html>
<html>
<head>
  <title>Ḥadīth Database</title>
  <meta name="viewport"
    content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <link href="/stylesheets/style.css" rel="stylesheet">
</head>
<body>
  <div class="col-lg-8 mx-auto p-3 py-md-5">
    <header class="d-flex align-items-center pb-3 mb-3 border-bottom">
      <a href="/"
        class="d-flex align-items-center text-dark text-decoration-none">
        <h1 class="fs-4">Ḥadīth Database</h1>
      </a>
    </header>

    <main class="row search-results">

      <div class="col-12 row d-flex justify-content-center align-items-center">
        <div class="col-md-6">
          <form action="/">
            <div class="form">
              <div class="input-group mb-3">
                <input name="q" type="text" class="form-control" value="<%=q%>"
                  placeholder="Search for aḥādīth">
                <button class="btn btn-outline-secondary" type="submit"
                  value=""><i class="fa fa-search"></i></button>
              </div>
            </div>
          </form>
        </div>
      </div>

    <% if (results.length != 1) { %>
      <div class="text-center">
        Found <%= results.length %> results for term: <%=q%>
      </div>
    <% } %>

    <% if (results.length > 0) { %> 
    <div class="row mb-2">
      <div class="col-md-6 col-sml-12 text-start">
        <% if (results[0].prev) {
        var result = results[0]; %>
        <a href="/<%= result.prev.book.alias %>:<%= result.prev.num %>">
          <small>&lt; <%= result.prev.book.shortNameEn %>:<%- result.prev.num %></small></a>
        <% } %>
      </div>
      <div class="col-md-6 col-sml-12 text-end">
        <% if (results[results.length-1].next) { 
        var result = results[results.length-1]; %>
        <a href="/<%= result.next.book.alias %>:<%= result.next.num %>">
          <small><%= result.next.book.shortNameEn %>:<%- result.next.num %> &gt;</small></a>
        <% } %>  
      </div>
    </div>
    <% } %>
    
    <div class="row h">
      <% for (var i = 0; i < results.length; i++) { %>
        <div class="col-md-6 col-sm-12">
          <p lang="en">
            <b><a href="/<%=results[i].book.alias%>:<%=results[i].num%>"><%= results[i].book.shortNameEn %> <%= results[i].num %></a></b> –
            <%- results[i].trans %></i><br />
            <em class="text-muted"><%- results[i].grade %></em>
          </p>
        </div>
        <div class="col-md-6 cols-sm-12">
          <p lang="ar">
            <b><a href="/<%=results[i].book.alias%>:<%=results[i].num%>"><%= results[i].book.shortNameAr %> <%= results[i].num.toArabicDigits() %></a></b> –
            <span class="text-muted"><%- results[i].chain %></span> <%- results[i].body %></i>
          </p>
        </div>
      <% } %>
    </div>

    </main>
  </div>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"
    crossorigin="anonymous"></script>
  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.bundle.min.js"
    crossorigin="anonymous"></script>
  <script src="https://kit.fontawesome.com/3c8decddde.js"
    crossorigin="anonymous"></script>
  <script src="/javascripts/script.js"></script>

</body>
</html>